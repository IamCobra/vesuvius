# docker-compose.yml
version: "3.8"

services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: vesuvius
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  # Optional: pgAdmin for database management
  pgadmin:
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@vesuvius.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8080:80"
    depends_on:
      - postgres

volumes:
  postgres_data:
#   # Stop services
# docker-compose down

# # Stop og slet volumes (fresh start)
# docker-compose down -v

# # Se logs
# docker-compose logs postgres

# # Connect direkte til database
# docker-compose exec postgres psql -U postgres -d vesuvius
